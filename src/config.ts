const server_port = 8080
const secret_key = "secret_key"

const db_host= ''
const db_port = 5432
const db_username = ''
const db_password = ''

export { secret_key, server_port, db_host, db_password, db_port, db_username }